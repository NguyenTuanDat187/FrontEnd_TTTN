<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="8dp"
    android:layout_marginHorizontal="4dp"
    app:cardElevation="6dp"
    app:cardCornerRadius="16dp"
    android:backgroundTint="@android:color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical">

        <!-- 🕒 Icon -->
        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/taikhoan"
        android:contentDescription="Icon lịch"
        android:background="@drawable/bg_circle_blue"
        android:padding="8dp"
        />

        <!-- 📋 Nội dung -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingStart="12dp">

            <TextView
                android:id="@+id/tvCareTask"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Uống thuốc buổi sáng"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/black"/>

            <TextView
                android:id="@+id/tvCareTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="08:00 AM - 08/06/2025"
                android:textSize="14sp"
                android:textColor="@color/black"/>

        </LinearLayout>

        <!-- ✅ Trạng thái -->
        <TextView
            android:id="@+id/tvCareStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Chưa làm"
            android:textColor="@color/material_dynamic_tertiary0"
            android:textStyle="bold"
            android:paddingStart="8dp"
            android:paddingEnd="4dp"/>
    </LinearLayout>
</androidx.cardview.widget.CardView>
